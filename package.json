{
  "name": "ranked-bedwars",
  "private": true,
  "scripts": {
    "build": "yarn clean && tsc",
    "clean": "rimraf ./build",
    "start": "node build/bot.js",
    "prod": "yarn build && yarn start"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "@types/node": "^16.4.5",
    "axios": "^0.21.1",
    "discord.js": "^12.5.3",
    "dotenv": "^10.0.0",
    "mongoose": "^5.13.3"
  }
}
